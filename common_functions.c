#include "common_functions.h"

/**
 * print_prompt - Prints the shell prompt.
 */
void print_prompt(void) {
    // Implementation of the print_prompt function
}

/**
 * handle_eof - Handles the "end of file" condition (Ctrl+D).
 * @input: The input string.
 */
void handle_eof(char *input) {
    // Implementation of the handle_eof function
}

/**
 * is_exit_command - Checks if the input is the exit command.
 * @input: The input string.
 * @return: 1 if the input is the exit command, 0 otherwise.
 */
int is_exit_command(char *input) {
    // Implementation of the is_exit_command function
}
